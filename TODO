# Lista de features para implementar

## target-scan
- Parametro para seleccionar opciones de puertos en target scan
- Mas modos de puertos:
nmap -Pn -n -T4 $TARGET_IP --open -p- -oN enum/port_scan.txt -sS
nmap -Pn -n -T4 $TARGET_IP -sCV -oN enum/versions_scan.txt -p

## nuevas features
- Funcion para activar VPN rapidamente

## Tmux
- Timers en tmux
- Macro para extraer las banderas automáticamente

## target-dirsearch
- dirsearch usando dirsearch si existe, ffuf como fallback
